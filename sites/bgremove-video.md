---
site_id: "bgremove-video"
name: "BGRemove"
production_url: "https://bgremove.video/"
changelog_url: "https://bgremove.video/changelog/"
delivery_method: "direct_publish"
target_repository: "not_applicable"
default_branch: "not_applicable"
updated_at: "2026-09-15"
---

# BGRemove 当前待办事项

## 已批准任务

### 把去绿幕首屏的静态绿幕原片改为真实动态视频

- 优先级：`P1`
- 页面或界面：英文及 `es`、`pt`、`de`、`fr` 五个 Remove Green Screen 页面在用户未登录时显示的首屏 Source / Alpha out 对比视频
- 当前问题与线上证据：2026-09-15 未登录状态线上复核显示，五个语言页面的 Source 均加载名为 `gs-hero-source...mp4` 的媒体文件，播放器时间会推进，但把对比滑块移到 89%、让 Source 几乎完整显示后，从约 0.05 秒观察到约 2.21 秒，绿幕人物画面没有可见动作或帧变化，实际观看效果等同于把一张静态人物图片封装成视频。相同时间段内右侧 Alpha out 的人物存在明显头部和表情动作，因此左右不是同一段逐帧对应的视频，无法演示“动态绿幕原片 → 同步透明结果”。
- 修改要求：保留当前右侧 Alpha out 视频中的同一人物、动作、服装、构图、时长、尺寸、帧率和播放节奏，生成或合成一份背景为均匀纯绿色、且人物动作与右侧逐帧一致的真实动态 Source 视频，替换五个去绿幕页面当前只有静态画面效果的 Source 媒体及对应 poster。绿色背景应清楚呈现为拍摄用绿幕，不加入房间、道具、渐变、纹理、阴影或文字；人物不得染绿、变形、换脸、重绘、裁切或改变比例。五种语言复用同一对视频素材，只本地化已有界面文字。
- 验收标准：清除缓存后，以未登录状态分别打开英文、西班牙语、巴西葡萄牙语、德语和法语去绿幕页，拖动滑块让 Source 完整显示并播放至少一个完整循环，绿幕人物在每个页面均持续呈现与 Alpha out 相同的头部、面部和身体动作，不是静止图片、重复单帧或仅有播放器时间推进。任意暂停到多个不同时间点时，左右人物轮廓、位置、比例和当前动作逐帧对应；播放、暂停、拖动和循环期间不出现时间偏移、跳帧、黑帧、旧室内背景、绿边加重或拉伸。poster 只用于视频载入前占位，视频可播放后必须被动态画面替代。桌面端和移动端均无溢出、异常裁切或布局变化。
- 不要修改：去绿幕页的 H1、正文、功能、额度、格式限制、透明输出、CTA、SEO 元数据、结构化数据、页面布局及已登录工作区；不替换人物、不修改右侧 Alpha out 视频、不使用首页普通背景示例、不修改首页对比视频。该修改属于示例素材修正，不新增或修改公开 Changelog。

### 修正西葡德 Blog 文章移动端署名与日期换行

- 优先级：`P2`
- 页面或界面：西班牙语、巴西葡萄牙语和德语的 9 个 Blog 文章页，在 390px 宽移动端视口下显示的组织署名、分隔符和发布日期
- 当前问题与线上证据：2026-09-15 未登录状态线上复核显示，五种语言的 15 个 Blog 文章页已完成已批准的 Meta description、署名链接和英文主图 alt 修正；可见署名均可聚焦并链接到当前语言 About 页面，五个 About 页面均可正常打开，`BlogPosting` 的 author 和 publisher 仍引用 BGRemove Organization，15 个页面也没有横向溢出。但在 390px 宽视口下，西班牙语、巴西葡萄牙语和德语全部 9 篇文章的署名位于第一行，`Publicado el`、`Publicado em` 或 `Veröffentlicht am` 加日期换到第二行；英文和法文 6 篇保持同一行。这不符合原验收标准中署名链接不得造成日期换行的要求。
- 修改要求：仅调整西班牙语、巴西葡萄牙语和德语 Blog 文章页的移动端署名元信息布局，使可见组织署名、现有分隔符、现有本地化发布日期前缀和日期在 390px 宽视口下保持同一行。保留当前署名文字及其 About 链接、发布日期文字和语义，不截断、不隐藏、不缩写内容；不得通过横向滚动实现。
- 验收标准：清除缓存后，以未登录状态在 390px 宽视口分别打开西班牙语、巴西葡萄牙语和德语的 9 个 Blog 文章页，组织署名、分隔符、发布日期前缀和日期均完整显示在同一行，署名链接仍可聚焦、可点击并指向当前语言 About 页面，页面宽度不超过视口；英文和法文 6 篇现有单行布局不变。桌面端五种语言的署名区域不出现拥挤、重叠、异常间距或布局变化。
- 不要修改：已经正确的西班牙语透明 WebM 文章 Meta description、15 个署名的文字与链接目标、文章日期与本地化前缀、两张英文主图 alt、其他语言图片 alt、Title、H1/H2、正文、摘要、URL、主图文件、canonical、`hreflang`、`BlogPosting`、Breadcrumb、Blog 列表页、产品功能页、导航、页脚或 Sitemap。该任务属于轻微移动端布局修正，不新增或修改公开 Changelog。
